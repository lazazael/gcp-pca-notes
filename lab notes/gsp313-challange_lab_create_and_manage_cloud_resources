# create all resources in the default region or zone, unless otherwise directed
# Naming normally uses the format team-resource; for example, an instance could be named nucleus-webserver1
# unless directed, use f1-micro for small Linux VMs, and use n1-standard-1 for Windows or other applications, such as Kubernetes nodes
# Create a cluster (in the us-east1-b zone) to host the service.
# Use the Docker container hello-app (gcr.io/google-samples/hello-app:2.0) as a # placeholder; the team will replace the container with their own work later.
# Expose the app on port

1.
gcloud auth list
gcloud config list project
gcloud config set compute/region us-east1
gcloud config set compute/zone us-east1-b

gcloud compute instances create _INSTANCE_NAME \
    --machine-type f1-micro \
    --image-family=debian-11 \
    --image-project=debian-cloud
2.
gcloud container cluster create --machine-type=n1-standard-1 nucleus-cluster
gcloud container cluster get-credentials nucleus-cluster
kebectl create deployment hello-server --image=gcr.io/google-samles/hello-app:2.0
kubectl expose deployment hello-server --port=_PORT
kubectl get service
3.
gcloud compute instances create 
